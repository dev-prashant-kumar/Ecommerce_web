import { Checkout_client } from "./Checkout_client";

export const metadata = {
  title: "Checkout | E-Store",
  description: "Complete your purchase",
};

export default function CheckoutPage() {
  return <Checkout_client />;
}